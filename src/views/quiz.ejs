<%- include("partials/header") %>

<main class="container py-4">
  <% for (const quiz of quizzList){%>

    <% for (const question of quiz.questions) {%>

  <h1 class="text-center m-2"> <%= question.title %> </h1>

<h2 class="small text-end"><%= quiz.author.firstname %> <%= quiz.author.lastname %> </h2>


<h3>Thèmes</h3>
  <ul class="mb-4 p-0">
    <li class="badge bg-primary">Astrophysique</li>
    <li class="badge bg-primary">Chimie</li>
  </ul>


<h3>Questions</h3>

  <span class="badge bg-info"><%= question.level %></span>
  <p class="fw-bold mb-1">Une molécule est constituée</p>
  <div class="mb-3 ms-2">
    <div>
      <input type="radio" id="#" name="#" value="#">
      <label for="#">d'atomes</label>
    </div>
    <div>
      <input type="radio" id="#" name="#" value="#">
      <label for="#">de tommes</label>
    </div>
    <div>
      <input type="radio" id="#" name="#" value="#">
      <label for="#">d'automne</label>
    </div>
  </div>
  <p class="fst-italic text-secondary"><%= question.anecdote %> <%= question.wiki %></p>


<button class="d-block mx-auto btn btn-primary mb-4">Envoyer</button>

<%}%>
<%}%>

  
</main>

<%- include("partials/footer") %>
